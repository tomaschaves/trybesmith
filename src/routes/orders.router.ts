// export const express = require('express');
// const orderController = require('../controllers/category.controller');
// const { checkToken, checkCategoryNameLength } = require('../middlewares/validations');

// const orderRouter = express.Router();
// orderRouter.post('/', checkToken, checkCategoryNameLength, categoryController.postCategory);
// orderRouter.get('/', checkToken, categoryController.getAllCategories);
// userRouter.get('/:id', checkToken, userController.getUser);

// module.exports = orderRouter;